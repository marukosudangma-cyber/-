<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>シンプルルーレット</title>
  <style>
    /* ほんとに最低限の見た目だけ */
    body {
      font-family: sans-serif;
      padding: 20px;
    }

    h1 {
      font-size: 20px;
      margin-bottom: 10px;
    }

    textarea {
      width: 100%;
      height: 80px;
    }

    button {
      margin-top: 10px;
      padding: 6px 12px;
    }

    #result {
      margin-top: 15px;
      font-size: 18px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>シンプルルーレット</h1>

  <p>カンマ（,）で区切って項目を入力してください。</p>
  <p>例：Aさん,Bさん,Cさん</p>

  <textarea id="itemsInput">1,2,3,4,5,6</textarea>
  <br>
  <button id="startBtn">スタート</button>

  <div id="result">結果：まだ未実行</div>

  <script>
    const itemsInput = document.getElementById("itemsInput");
    const startBtn = document.getElementById("startBtn");
    const resultDiv = document.getElementById("result");

    startBtn.addEventListener("click", function () {
      // テキストをカンマで分割
      const text = itemsInput.value;
      const rawItems = text.split(",");

      // 余計な空白を消して、空文字は除外
      const items = rawItems
        .map(function (item) {
          return item.trim();
        })
        .filter(function (item) {
          return item.length > 0;
        });

      // 何もない場合のエラー処理
      if (items.length === 0) {
        alert("項目を入力してください。");
        return;
      }

      // ランダムに1つ選ぶ
      const randomIndex = Math.floor(Math.random() * items.length);
      const selected = items[randomIndex];

      // 結果を表示
      resultDiv.textContent = "結果：" + selected;
    });
  </script>
</body>
</html>
