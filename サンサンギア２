<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>入浴剤ルーレット</title>

  <style>
    /*CSS */
    body {
      font-family: sans-serif;
      text-align: center;
      padding: 20px;
      background-color: #ffffff;
      color: #000000;           
    }

    h1 {
      font-size: 24px;
      margin-bottom: 10px;
    }

    p {
      font-size: 16px;
      margin: 6px 0;
    }

    button {
      padding: 8px 16px;
      font-size: 16px;
      background-color: #f0f0f0; 
      border: 1px solid #000000;
      cursor: pointer;
      margin-top: 15px;
    }

    #result {
      font-size: 20px;
      margin-top: 20px;
      font-weight: bold;
    }
  </style>
</head>

<body>
  <h1>入浴剤ルーレット</h1>

  <p>次の中からランダムに1つ選びます：</p>
  <p>豊後・信州・十和田・箱根・草津</p>

  <button id="startBtn">スタート</button>

  <p id="result">結果：まだ未実行</p>

  <script>
    var items = ["豊後", "信州", "十和田", "箱根", "草津"];

    document.getElementById("startBtn").onclick = function () {
      var randomIndex = Math.floor(Math.random() * items.length);
      var selected = items[randomIndex];
      document.getElementById("result").textContent = "結果：" + selected;
    };
  </script>
</body>
</html>

   

     
